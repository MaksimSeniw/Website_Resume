runtime: nodejs22

entrypoint: npm start

env_variables:
  NODE_ENV: "production"

handlers:
  - url: /public
    static_dir: public

  - url: /.*
    script: auto

automatic_scaling:
  min_instances: 0
  max_instances: 5
  target_cpu_utilization: 0.65